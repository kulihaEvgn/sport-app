# Human review: T-003 Prisma

| **Status** | `PENDING` |

## Что сделано

- Schema: User, Exercise, Program, ProgramExercise
- `lib/prisma.ts`, seed 30 упражнений
- Prisma **6** (не 7 — проще для старта)
- `docs/DATABASE.md` — как поднять БД

## Подключи БД (один раз)

**Docker:** `docker compose up -d` → `npm run db:push` → `npm run db:seed`

**Neon:** вставь `DATABASE_URL` в `.env` → те же команды

## Проверка

`npx prisma validate` — уже ок у разработчика.

После БД: `/profile` → блок «База данных» с User ID.

**APPROVED** → T-005 (упражнения UI)
