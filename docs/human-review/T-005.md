# Human review: T-005 Exercises API + UI

| **Status** | `PENDING` |

## Проверка

1. `/exercises` открывается, показывается список упражнений из БД
2. Фильтр «Грудь» показывает только CHEST
3. Создание/редактирование/удаление своего упражнения работает
4. В каталоге (seed) нет кнопок edit/delete
5. `curl http://localhost:3100/api/exercises` без заголовка Authorization возвращает 401

**APPROVED** → следующий тикет
